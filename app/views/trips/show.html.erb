<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @trip.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @trip.description %>
</p>

<h2>Legs</h2>
<% @legs = @trip.legs %>
<%= render :partial => 'legs/list' %>

<h2>Add a New Leg</h2>
<%= render 'legs/form' %>

<%= link_to 'Edit', edit_trip_path(@trip) %> |
<%= link_to 'Back', trips_path %>
